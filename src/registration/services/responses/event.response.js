export class EventResponse {
  constructor(
    id,
    name,
    description,
    schedule_at 
  ) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.schedule_at = schedule_at;
  }
}
